<Application x:Class="AppUsageTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps.Metro core styles, fonts and a default accent/theme.
                     Swap Light.Blue for any other Themes/*.xaml if you want a
                     different accent color, or move to MahApps' ThemeManager
                     at runtime later if you want a theme switcher. -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Seamless, tileable notebook-paper texture used as the main
                 window's background: a soft paper fill with faint ruled
                 lines, built as vector geometry (not an image) so it stays
                 crisp at any window size or DPI. Defined at the app level
                 so it's guaranteed to be loaded before any window that
                 references it. -->
            <DrawingBrush x:Key="NotebookBackgroundBrush"
                          TileMode="Tile"
                          Viewport="0,0,32,32" ViewportUnits="Absolute"
                          Stretch="None">
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#FFFCFBF7">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,32,32" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Pen>
                                <Pen Brush="#33A9C6E0" Thickness="1" />
                            </GeometryDrawing.Pen>
                            <GeometryDrawing.Geometry>
                                <LineGeometry StartPoint="0,31.5" EndPoint="32,31.5" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </ResourceDictionary>
    </Application.Resources>
</Application>
